# 项目实现跟踪（Career Agent）

更新时间：2026-04-17

## 1. 里程碑

1. M1 基础工程与数据接入。
2. M2 核心能力（画像、匹配、报告）完成。
3. M3 图谱与路径规划完成。
4. M4 前后端联调、验收与大赛材料整理。

## 2. 当前状态总览

1. 后端基础骨架：已完成（已切换为 Express + TypeScript + PostgreSQL 主存储）。
2. 岗位数据导入：已完成。
3. 岗位画像生成：已升级为 V2（已切换为 Agent 强制生成，调用失败直接记失败，不再回退 LLM/规则；`AGENT_MODEL` 必须命中已配置模型，未配置时直接报错；已修复 `agent:smoke` 假阳性并支持鉴权失败快速终止；支持后台实时日志输出避免“卡住”误判）。
4. 学生画像：已完成（支持表单录入与简历上传，输出结构化画像 + 完整度/竞争力评分）。
5. 人岗匹配评分：已升级为 V2（新增路径建议与证据引用字段）。
6. 职业报告生成与导出：已升级为 V2（模板正文 + 结构化章节 + 行动计划 + PDF 导出；独立 LLM 链路已移除）。
7. qmd-RAG 管线：进行中（已完成知识库模块、岗位/项目文档索引脚本与检索基础能力）。
8. Pi Coding Agent 编排层：进行中（已切换为真实 Pi SDK 运行时，Agent 通过业务工具执行任务；默认独立配置目录已落地，并兼容从 `~/.openclaw/agents/main/agent` 一次性导入缺失认证/模型文件；2026-04-02 已移除独立 llm-chat 运行链路，补上 `AGENT_MODEL` 自动推断，并用 `agent:smoke` 完成最小调用验证；同时已将“禁止重新引入 Chat Completions”写入长期架构规范）。
9. 图谱与路径规划：已升级为 V2（支持自动构图：晋升边 + 换岗边 + 技能迁移边 + 边分值/技能缺口入图；2026-04-05 新增“从 `v2_manual_job_portraits` 一键生成图谱”后端接口与前端触发按钮）。
10. 前端流程页面：进行中（已新增数据处理中心、岗位画像中心，并切换到 `/api/v2` 链路；2026-04-04 已恢复岗位画像中心前端入口与 `/api/v2/canonical-roles` 展示页，并为历史前端调用增加 `/api/v2/job-profiles` 兼容桥接；同日已将前端岗位画像页彻底切换为 canonical 接口，不再依赖兼容层；前台主题已移除 dark 模式，统一仅保留 light 模式）。
11. 指标评测与验收：已完成（已落地 30 条手工抽样评测、阈值判定与 Markdown 报告输出）。
12. 运行时环境统一：已完成（仓库根目录、前后端 workspace 已统一要求 Node 22.20+，补充 `.nvmrc` / `.node-version`）。
13. Pi 作为系统 AI 中枢实施拆解：进行中（已新增独立 process 文档，落地首版后端 `ai` 模块骨架、`/api/v2/ai` 统一入口，并完成 Pi 运行时到 `ai/runtime`、业务工具到 `ai/tools` 的首轮结构抽离；同时已支持优先复用标准 `~/.pi/agent` 中的 `auth.json/models.json` 做项目登录引导，并按 openclaw 风格接入 Kimi；2026-04-04 已进一步把默认 Agent 主链路切换到 `kimi-coding/k2p5`，`agent:auth:kimi` 默认写入 `kimi-coding` provider，且本机 `agent:smoke` 已返回 `AGENT_OK`；report 模块的独立 LLM 链路也已删除）。
14. 1W 岗位数据资产化执行：进行中（已落地首版 todoList、补齐 `raw_job_posts/job_normalized/job_categories/career_nodes/career_edges` 主库建表，以及 Python 清洗归类脚本与根命令触发入口；2026-04-04 已补齐 `jobs_cleaned.csv -> job_normalized` 幂等导入脚本与根命令，形成离线到在线闭环）。
15. 1W 岗位画像联动（D05）：已完成首版（`jobs-intelligence` 流水线已接入 `job_normalized` 提示输入，Agent 画像生成优先消费标准岗位族/标题提示，并在任务进度日志中暴露 `normalized_hint` 命中计数）。
16. 1W 匹配联动（D06）：已完成首版（`matching` 模块已读取岗位标准化提示，并将岗位族/标准标题/归一置信度证据写入 `evidence_refs`，供报告层直接复用）。
17. 1W 报告联动（D07）：已完成首版（模板报告已合并匹配证据与路径差距证据，`career_path` 章节补充关键技能展示，支撑可解释报告输出）。
18. 数据库排障与重导：已完成（清理岗位域旧数据与僵尸流水线任务后，使用 `data/岗位数据.xls` 重新导入，结果 `imported=9958, skipped=0`；重导后 `jobs=9958`、重复分组为 0）。
19. 1W 端到端一致性验收（D09）：已完成首版（新增 `evaluation-normalized-e2e` 脚本，抽样校验画像岗位族与匹配/报告证据是否命中 `job_normalized` 提示，并支持空样本告警报告输出）。
20. P07 工程化首轮：已完成首版（`jobs-intelligence` 流水线由串行改为受控并发 worker 池，并新增可配置指数退避重试；`env` 新增 `JOBS_PIPELINE_CONCURRENCY/JOBS_PIPELINE_RETRY_*` 配置；补充并发与重试 TDD 用例，`npm run type-check` 通过）。
21. P07 工程化第二轮：已完成首版（新增失败审计与重试队列表 `v2_pipeline_failures/v2_pipeline_retry_queue`；流水线失败时写审计并对可重试错误入队；新增 `/api/v2/jobs/pipeline/tasks/:task_id/failures` 与 `/api/v2/jobs/pipeline/retry-queue` 查询接口；相关 service 测试与 type-check 通过）。
22. P07 工程化第三轮：已完成首版（新增重试队列消费接口 `POST /api/v2/jobs/pipeline/retry-queue/process`，支持按 `next_run_at` 领取任务并执行 `claim -> processing -> done/failed/pending` 状态流转；新增 pending 去重唯一约束，补齐消费流程 TDD，`jobs-intelligence` 服务测试与 `npm run type-check` 通过）。
23. P07 工程化第四轮：已完成首版（`facts/canonical` 流水线新增可观测性漏斗：`latest -> eligible -> groups`，并输出低置信度/缺证据过滤统计与 top role group 分布；任务 `message/error_message` 已补充诊断摘要，支持直接定位“为什么 canonical 数量偏少”）。
24. P08 扩产与归一第五轮：已完成首版（`job_family` 归一新增“其他岗位”兜底替换与别名映射，`job_level` 升级为标题+经验+薪资联合推断；重跑任务 `#14` 后 `canonical_groups=24`，满足“标准岗位 >= 10”，且本轮新增结果不再出现 `其他岗位` 分组）。
25. 人工岗位画像直出：已完成首版（新增 `v2_manual_job_portraits` 表、`/api/v2/job-portraits/manual` 查询接口与 `job-portraits:seed` 脚本；前端岗位画像中心已切换为展示数据库中的 10 条人工画像）。
26. 学生画像中心前端重构：已完成并增强输入约束（页面重构为“表单录入 / 简历上传”二选一交互，切换为纯前端本地预览模式，暂不依赖后端接口；已将专业技能、证书、创新能力、学习能力、抗压能力、沟通能力、实习能力改为表单输入项并纳入提交校验，不再仅做前端静态展示）。
27. 路径图谱比赛演示链路：已完成（前端“生成图谱”按钮改为演示模式：展示“正在调用Agent生成图谱”动画后直接读取数据库预置图谱；当目标岗位图谱缺失时自动回退到预置岗位图谱，保证页面稳定可展示）。
28. 人工岗位画像批次替换（计算机向）：已完成（基于 `jobs_cleaned.csv` 高频岗位重新构建 10 条更偏计算机方向的人工画像，覆盖写入 `v2_manual_job_portraits`；通过 `job-portraits:seed` 脚本完成落库，前端岗位选择数据源保持 `/api/v2/job-portraits/manual` 直连数据库展示）。
29. 职业报告页面美化与可读性修复：已完成（报告章节改为默认预览态展示，新增“预览/编辑”模式切换；修复章节内容以小文本框展示的问题，统一优化章节卡片、证据引用与行动计划样式，满足比赛演示观感要求）。
30. Pi Coding 页面下线：已完成（移除前端 `/agent` 路由与 Pi Agent 页面文件，同时清理侧边导航和控制台入口中的“Pi Agent”链接，避免用户误入无关页面）。
31. 岗位画像生产中心演示化：已完成（将“开始生成画像”改为纯前端动态演示流程，模拟排队/清洗/生成/汇总进度，不再调用后端任务接口，不再写入数据库，页面明确展示“演示模式不落库”状态）。
32. 路径推荐岗位名称展示修复：已完成（“分析洞察-路径推荐”中将 `job-xxxx` 节点 ID 自动映射为图谱节点岗位名称，路径标题与摘要均按岗位名展示，避免用户看到原始 ID）。
33. 简历画像目标岗位归一修复：已完成（学生画像简历模式新增数据库岗位建议输入；后端在收到“待定岗位”等占位词时，会优先从简历标签与技能中推断目标岗位，并映射为数据库中最接近的岗位标题；同时修复 `Name /AAAA...` 这类 PDF 字体元信息被误识别为姓名的问题，新增 profile service 回归测试并通过）。
34. 简历上传真实解析与液态玻璃改版：已完成（后端 `profile` 路由不再把 PDF/DOCX 二进制直接当 UTF-8 文本，而是接入 `pdftotext` / `textutil` 做真实文本提取；姓名提取新增“顶部标题行”兜底；前端学生画像中心新增“当前选中画像”预览态，历史列表支持显式切换预览并高亮当前项；整页视觉升级为液态玻璃风格）。
35. 前端全站液态玻璃主题统一：已完成（`AppLayout` 已升级为统一液态玻璃导航壳层与全局设计令牌；`dashboard / data-pipeline / profile / job-profiles / career-path / matching / report` 页面已统一到半透明渐变、强模糊、边缘高光、发光选中态和同源按钮输入样式，形成一致的比赛演示视觉语言）。
36. 简历姓名提取兜底增强：已完成（`profile.service` 新增“标题复合行拆解 + 文件名兜底”双重姓名识别策略，兼容 `李晓彤 | 后端开发工程师` 与 `张三-后端开发工程师-简历.pdf` 等常见简历格式；新增 2 条回归测试，避免继续落成“匿名候选人”）。
37. 图片简历多模态解析接入：已完成（`/api/v1/profile/resume` 已支持图片简历直接调用 Pi Agent 多模态 `session.prompt(..., { images })` 解析姓名与正文转写；对扫描版 PDF，当 `pdftotext` 无法正常提取文本时会自动回退到 Agent 视觉解析第一页，再沿用既有画像生成链路）。
38. PDF 方括号姓名清洗修复：已完成（修正 `profile.service` 对标题行的方括号清洗逻辑，由“整段删除”改为“去壳保内容”，兼容 `[ 吴友桃 ] | 全栈开发工程师` 这类 PDF 转文本格式；新增对应回归测试）。
39. 岗位画像漫画 MVP：已完成首版（后端新增 `/api/v2/job-portraits/manual/:job_name/comic`，同步调用本机 `baoyu-imagine` 生成单张四格漫画并保存到本地静态目录；前端岗位画像中心支持无图生成、有图展示和重新生成）。

## 3. 架构落地进度

| 架构层       | 目标技术方案                                   | 当前落地情况                                                                                                                                                                                                                                                         | 状态               |
| ------------ | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| 前端展示层   | Vue3 + TypeScript + Element Plus + ECharts/G6  | 已迁移为 app/features/shared 结构，dashboard/profile/matching/report/career-path 页面已落地，支持报告导出与图谱展示                                                                                                                                                  | 进行中             |
| 后端服务层   | Express + TypeScript + REST API(v2)            | 已新增 jobs-intelligence 域，落地 `/api/v2/jobs/pipeline`、`/api/v2/career-paths` 以及 v2 匹配/报告路由；2026-04-04 已移除逐条岗位画像生成模式与 `/api/v2/job-profiles*` 查询入口                                                                                    | 进行中             |
| 智能处理层   | Pi Coding Agent 主链路，负责画像/匹配/报告编排 | 已切换为真实 Pi SDK + 业务工具接入，支持独立 Agent 配置目录、旧目录兼容导入、工具轨迹与结果快照；2026-04-02 已移除独立 llm-chat，旧 `/api/v1/agent/chat` 已回指当前任务型 Agent，`agent:smoke` 已验证通过，并已在工程/架构文档中明确禁止恢复 Chat Completions 双链路 | 进行中             |
| 关系数据层   | PostgreSQL                                     | jobs/profile/matching/report/report_export/agent 已全部切换到 PostgreSQL repository                                                                                                                                                                                  | 已完成             |
| 文档处理层   | qmd（结构化切分）+ Marker/PyMuPDF              | 已新增 knowledge 模块、文档切分逻辑与内部项目文档索引脚本                                                                                                                                                                                                            | 进行中             |
| 向量检索层   | pgvector                                       | 已新增 PostgreSQL/pgvector 知识库存储、混合检索接口与初始化脚本                                                                                                                                                                                                      | 进行中             |
| 图谱关系层   | Neo4j                                          | 已升级为自动构图（CareerRoleV2 + CAREER_PATH_V2），支持全量覆盖写与子图查询                                                                                                                                                                                          | 进行中             |
| 运维与评测层 | 日志、评测集、准确率报表                       | 已新增知识检索基线评测接口与脚本，并补齐 30 条手工抽样评测数据、统计脚本与报告输出                                                                                                                                                                                   | 已完成（当前阶段） |

## 4. 工作分解与跟踪

| 编号 | 模块   | 任务                                    | 状态                                                                                                                                                                                                                                                                                                         | 优先级 | 负责人 | 计划完成   | 验收标准                                                                     |
| ---- | ------ | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ------ | ---------- | ---------------------------------------------------------------------------- |
| T01  | 后端   | Express + TypeScript 工程结构与启动入口 | 已完成                                                                                                                                                                                                                                                                                                       | P0     | 待分配 | 2026-04-01 | 服务可启动，`/healthz` 可访问                                                |
| T02  | 数据   | 岗位数据导入接口（文件上传+解析）       | 已完成                                                                                                                                                                                                                                                                                                       | P0     | 待分配 | 2026-04-01 | 支持 CSV/JSON/XLSX/XLS 导入                                                  |
| T03  | 数据   | 岗位数据查询接口                        | 已完成                                                                                                                                                                                                                                                                                                       | P0     | 待分配 | 2026-04-01 | 支持关键字与分页                                                             |
| T04  | 能力   | 岗位画像生成接口                        | 已完成（规则基线）                                                                                                                                                                                                                                                                                           | P0     | 待分配 | 2026-04-01 | 可生成结构化岗位画像                                                         |
| T05  | 能力   | 学生画像接口（简历上传）                | 已完成                                                                                                                                                                                                                                                                                                       | P0     | 待分配 | 2026-04-02 | 输出能力维度与完整度评分                                                     |
| T06  | 能力   | 学生画像接口（表单录入）                | 已完成（支持结构化字段、四维分、完整度与竞争力评分）                                                                                                                                                                                                                                                         | P0     | 待分配 | 2026-04-01 | 输出结构化画像                                                               |
| T07  | 能力   | 人岗匹配四维评分                        | 已完成（支持结果解释与复现缓存）                                                                                                                                                                                                                                                                             | P0     | 待分配 | 2026-04-02 | 输出四维得分、总分、差距                                                     |
| T08  | 能力   | 职业报告生成                            | 已完成（MVP：按 match_id 生成多版本报告）                                                                                                                                                                                                                                                                    | P0     | 待分配 | 2026-04-02 | 生成可执行行动计划                                                           |
| T09  | 能力   | 报告编辑与导出                          | 已完成（已支持结构化编辑与 PDF 导出，Docx 延后）                                                                                                                                                                                                                                                             | P1     | 待分配 | 2026-04-02 | 支持编辑与 PDF 导出（Docx 延后）                                             |
| T10  | 图谱   | 垂直晋升路径                            | 已完成（首轮 Neo4j 闭环，已覆盖首批规范岗位晋升链路）                                                                                                                                                                                                                                                        | P1     | 待分配 | 2026-04-02 | 至少覆盖核心岗位集                                                           |
| T11  | 图谱   | 换岗路径规划                            | 已完成（已为 5+ 岗位提供每岗 >=2 条换岗路径）                                                                                                                                                                                                                                                                | P1     | 待分配 | 2026-04-02 | 5 岗位 \* 每岗 >=2 路径                                                      |
| T12  | 前端   | 上传/匹配/报告页面联调                  | 已完成（上传/匹配/报告页已联调，PDF 导出可演示）                                                                                                                                                                                                                                                             | P1     | 待分配 | 2026-04-02 | 可演示全流程                                                                 |
| T13  | 评测   | 80%/90% 指标评测方案                    | 已完成（30 条手工抽样样本 + JSON 汇总 + Markdown 报告）                                                                                                                                                                                                                                                      | P1     | 待分配 | 2026-04-02 | 抽样评测脚本可复算并产出报告                                                 |
| T14  | 交付   | PPT/方案/演示视频/知识库                | 待开始                                                                                                                                                                                                                                                                                                       | P1     | 待分配 | 待定       | 满足比赛提交要求                                                             |
| T15  | RAG    | qmd 文档解析与切分管线接入              | 进行中（已完成知识库模块、切分逻辑和索引脚本）                                                                                                                                                                                                                                                               | P0     | 待分配 | 待定       | 可输出结构化 chunk 与元数据                                                  |
| T16  | RAG    | pgvector 检索与召回评测                 | 进行中（已完成混合检索 API、基线评测脚本，并新增 `knowledge:baseline` 一键快照输出 `data/evaluation/knowledge/latest.json` + 时间戳归档；已补齐评测命中文本归一化与更贴近现网数据的评测查询样例）                                                                                                            | P0     | 待分配 | 待定       | 召回评测报告可复现                                                           |
| T17  | Agent  | Pi Coding Agent 编排接入                | 进行中（已完成真实 Pi SDK 接入、业务工具封装、独立配置目录和旧目录兼容导入，并补上 `AGENT_MODEL` 自动推断；2026-04-02 已移除 llm-chat、完成 `agent:smoke` 验证，并补充“禁止恢复 Chat Completions”文档约束）                                                                                                  | P0     | 待分配 | 待定       | 支持稳定跑通任务规划-执行-结果汇总链路                                       |
| T18  | Agent  | 工具调用审计与失败重试机制              | 进行中（已接入步骤轨迹、知识检索降级、报告失败降级与总结兜底，待结合真实配置验证）                                                                                                                                                                                                                           | P1     | 待分配 | 待定       | 失败任务可降级继续且有审计记录                                               |
| T19  | 数据   | 结构化数据存储切换到 PostgreSQL         | 已完成（移除 JSON repository，jobs/profile/matching/report/report_export/agent 全量切换为 PostgreSQL）                                                                                                                                                                                                       | P0     | 待分配 | 2026-04-03 | 默认运行链路不再依赖任何 JSON 存储                                           |
| T20  | 数据   | V2 岗位智能流水线（手动触发）           | 已完成（新增 `jobs-intelligence` 域、任务表、全量/增量手动触发脚本）                                                                                                                                                                                                                                         | P0     | 待分配 | 2026-04-03 | 支持 `POST /api/v2/jobs/pipeline/run` 与任务查询                             |
| T21  | 能力   | V2 岗位画像（Agent 强制生成）           | 已下线（2026-04-04 已移除 jobs-intelligence 的逐条岗位画像生成模式与 `/api/v2/job-profiles*` 查询入口，流水线仅保留 facts/canonical 聚合）                                                                                                                                                                   | P0     | 待分配 | 2026-04-04 | 不再支持逐条岗位画像批量生成链路                                             |
| T22  | 图谱   | V2 自动图谱构建                         | 已完成（基于技能相似度与岗位层级自动构建晋升/换岗边并写入 Neo4j）                                                                                                                                                                                                                                            | P0     | 待分配 | 2026-04-03 | 支持 `/api/v2/career-paths/jobs/{job_id}`                                    |
| T23  | 能力   | 匹配与报告 V2 契约升级                  | 已完成（匹配增加路径建议/证据；报告增加 generator_mode/evidence/action_plan）                                                                                                                                                                                                                                | P0     | 待分配 | 2026-04-03 | `/api/v2/matches` 与 `/api/v2/reports` 正常可用                              |
| T24  | 前端   | 信息架构重做（V2）                      | 已完成首版（新增数据处理中心/路径图谱中心，既有页面切到 v2 API；2026-04-04 曾下线岗位画像中心导航入口，现已补回“岗位画像中心”路由/导航/控制台入口，并接入 `/api/v2/canonical-roles` 列表展示；前端页面已彻底切到 canonical，不再调用 `/api/v2/job-profiles*`）                                               | P1     | 待分配 | 2026-04-04 | 全链路页面可联调用于演示                                                     |
| T25  | Agent  | Pi 作为系统 AI 中枢实施清单             | 已完成首版（新增 `docs/Pi作为系统AI中枢实施清单.md`，完成阶段拆分、最小闭环建议、风险与完成定义）                                                                                                                                                                                                            | P0     | 待分配 | 2026-04-04 | 后续实施具备统一执行顺序、交付物与验收标准                                   |
| T26  | Agent  | AI 中枢统一入口首版落地                 | 已完成首版（新增 `apps/backend/src/modules/ai` 模块骨架，挂载 `/api/v2/ai`，前端 Agent 页面已切到新入口，并同步共享类型与 OpenAPI）                                                                                                                                                                          | P0     | 待分配 | 2026-04-04 | 后端存在统一 AI 入口且前端主调用链可直接命中 `/api/v2/ai/tasks`              |
| T27  | Agent  | Pi Runtime 与业务工具目录抽离           | 已完成首版（新增 `apps/backend/src/modules/ai/runtime` 与 `apps/backend/src/modules/ai/tools`，旧 `agent.pi.ts` 已收敛为兼容导出层）                                                                                                                                                                         | P0     | 待分配 | 2026-04-04 | Pi 会话装配与业务工具定义不再继续堆在单个兼容文件中                          |
| T28  | Agent  | auth.json 登录兼容标准 Pi 目录          | 已完成（独立 Agent 目录缺少凭证时，运行时/岗位画像流水线/smoke 脚本会优先从 `~/.pi/agent` 自动导入 `auth.json` 与 `models.json`）                                                                                                                                                                            | P0     | 待分配 | 2026-04-04 | 用户先用官方 `pi` 登录一次后，项目可直接复用该登录态                         |
| T29  | Agent  | Kimi API 写入项目 auth.json             | 已完成（`agent:auth:kimi` 可按当前 `AGENT_MODEL` 写入对应 provider 凭证；切换到 `kimi-coding/k2p5` 后已成功写入 `kimi-coding`，并通过本机 `agent:smoke` 验证）                                                                                                                                               | P0     | 待分配 | 2026-04-04 | 项目可通过独立 `auth.json` 直接使用 Kimi Code                                |
| T30  | Agent  | 对齐 OpenClaw 的 Kimi Provider 语义     | 已完成首版（新增共享 Agent 引导工具；可从 `auth-profiles.json` 转换导入 `moonshot/kimi-coding` 凭证；`agent:auth:kimi` 现按 provider 区分 `MOONSHOT_API_KEY` 与 `KIMI_API_KEY/KIMICODE_API_KEY`，并补齐 openclaw 风格的 Moonshot 模型配置）                                                                  | P0     | 待分配 | 2026-04-04 | 当前项目的 Kimi/Moonshot 接入语义与 OpenClaw 保持一致                        |
| T31  | Agent  | 默认链路切换为 Kimi Code                | 已完成（环境默认值、本机 `.env`、初始化脚本默认模型、自检错误提示与使用文档已统一切到 `kimi-coding/k2p5`；Moonshot 仅保留为显式兼容选项，且 smoke 已命中 `kimi-coding/k2p5`）                                                                                                                                | P0     | 待分配 | 2026-04-04 | 当前项目在不额外指定模型时默认走 Kimi Code                                   |
| T32  | Agent  | 删除报告独立 LLM 链路                   | 已完成（`report.module` 已改为只注入模板生成器，`llm-report.generator.ts` 与 `shared/ai/openai-compatible.ts` 已删除，报告契约中的 `generator_mode` 已收缩为 `template`）                                                                                                                                    | P0     | 待分配 | 2026-04-04 | 报告能力仅通过 Agent 的 `create_report` 工具进入，不再存在独立模型直连       |
| T33  | 数据   | 1W 岗位资产分层表首版落地               | 已完成首版（`ensureCareerCoreSchema` 新增 `raw_job_posts/job_categories/job_normalized/career_nodes/career_edges` 与基础索引，服务启动即可自动建表）                                                                                                                                                         | P0     | 待分配 | 2026-04-04 | 数据资产分层核心表具备可创建能力                                             |
| T34  | 数据   | Python 清洗归类脚本首版                 | 已完成并增强（新增 `scripts/job_data_asset_pipeline.py`，支持字段别名归一；根命令 `data:pipeline:prepare` 默认切到 `data/岗位数据.xls` 全量输入，同时保留 `data:pipeline:prepare:sample` 样例入口；本机验证全量结果：输入 9958 -> 清洗后 9164）                                                              | P0     | 待分配 | 2026-04-04 | 可稳定跑通“全量清洗 -> 归类 -> 落盘”，并兼容样例快速验证                     |
| T35  | 数据   | 1W 岗位标准化提示接入流水线             | 已完成并调整（`listPipelineJobs` 关联 `job_normalized` 返回岗位族/标题/置信度提示；逐条画像生成链路已下线，提示信息用于事实抽取与 canonical 聚合质量观测）                                                                                                                                                   | P0     | 待分配 | 2026-04-04 | 流水线处理时可显式消费标准化提示并在日志中可观测                             |
| T36  | 数据   | 1W 岗位标准化证据接入匹配结果           | 已完成首版（`matching` 仓储新增 `getNormalizedJobHint`，服务层将岗位族/标准标题/归一置信度合并写入 `evidence_refs`；`npm run type-check:backend` 已通过）                                                                                                                                                    | P0     | 待分配 | 2026-04-04 | 匹配结果可稳定产出标准岗位证据，供报告与解释复用                             |
| T37  | 数据   | 1W 岗位证据接入报告模板                 | 已完成首版（`template-report.generator.ts` 已合并 `match.evidence_refs`、`gaps.evidence` 与路径技能差距证据，路径章节补充关键技能展示；`npm run type-check:backend` 已通过）                                                                                                                                 | P0     | 待分配 | 2026-04-04 | 报告可显式引用岗位标准化证据与路径依据                                       |
| T38  | 数据   | 数据库污染排查与全量重导                | 已完成（清理 `jobs` 相关衍生表与僵尸流水线任务，保留 `project_doc` 知识；通过 `/api/v1/jobs/import` 重导 `data/岗位数据.xls`，结果 `imported=9958` 且重复分组 0）                                                                                                                                            | P0     | 待分配 | 2026-04-04 | 岗位主表恢复干净可用状态，支持后续流水线重新生成                             |
| T39  | 数据   | 1W 清洗结果入库闭环（job_normalized）   | 已完成并修复编码问题（`job-normalized-import` 对 CSV 改为 UTF-8 字符串读取，避免中文乱码；UPSERT 新增 `source_job_code/source_row_id` 匹配兜底，支持修复历史脏数据；根命令保留 `data:pipeline:import/sync`）                                                                                                 | P0     | 待分配 | 2026-04-04 | 中文字段可稳定入库且不再出现乱码，导入具备更强幂等修复能力                   |
| T40  | 数据   | 1W 岗位标准化证据端到端验收脚本         | 已完成首版（新增 `apps/backend/src/scripts/evaluation-normalized-e2e.ts`，抽样校验 `v2_job_profiles/match_results/career_reports` 对 `job_normalized` 岗位族提示的一致性；根命令新增 `evaluation:normalized:e2e`，并支持空样本告警）                                                                         | P0     | 待分配 | 2026-04-04 | 可一键执行一致性验收并产出 Markdown 报告                                     |
| T41  | 后端   | P07 重试队列消费与状态流转              | 已完成首版（新增 `/api/v2/jobs/pipeline/retry-queue/process`，服务层支持 `claim -> processing -> done/failed/pending`，可重试失败按退避回写 pending 并累计 attempts；仓储层新增 pending 去重唯一索引）                                                                                                       | P0     | 待分配 | 2026-04-04 | 重试任务可被领取并闭环处理，避免重复 pending 入队                            |
| T42  | 后端   | facts 证据兜底修复（空描述场景）        | 已完成（`extractPostingProfileFacts` 在证据全空时自动回填 `title/company_intro` 最小证据；重跑 `jobs:pipeline:run` 后 `canonical_roles=6`，岗位画像中心恢复可见）                                                                                                                                            | P0     | 待分配 | 2026-04-04 | 空描述数据集也可稳定产出 canonical role，不再出现 `canonical_roles=0`        |
| T43  | 后端   | canonical 诊断漏斗与分组可观测性增强    | 已完成首版（流水线新增 `latest/eligible/low_conf/missing_evidence` 漏斗统计与 `groups/singleton/p50/p90/max` 分组分布日志；任务完成消息与降级错误已携带可追溯诊断摘要）                                                                                                                                      | P0     | 待分配 | 2026-04-04 | 可直接从任务状态判断 canonical 偏少来源，无需额外连库排查                    |
| T44  | 后端   | canonical 扩产与岗位族归一修复          | 已完成首版（抽取阶段新增岗位族提示归一与“其他岗位”替换；职级推断引入薪资/经验信号；列表查询默认过滤历史 `其他岗位`；任务 #14 实测 `canonical_groups=24`）                                                                                                                                                    | P0     | 待分配 | 2026-04-04 | 标准岗位画像不少于 10 且新增结果不出现“其他岗位”                             |
| T45  | 后端   | canonical 同名去重与画像补全修复        | 已完成首版（分组层级由 `L1-L4` 收敛为 `L2/L4` 双档，显著减少同名岗位多层级重复；画像聚合新增岗位族兜底技能/工具/职责填充；流水线执行前清理历史残留 role_key，避免旧分组混入当前结果。任务 #16 实测 `canonical_groups=12`、空字段计数归零）                                                                   | P0     | 待分配 | 2026-04-04 | canonical 列表无历史脏重复，且 `common_skills/职责/tools` 不再批量为空       |
| T46  | 后端   | canonical 同名分层画像差异化修复        | 已完成首版（`buildCanonicalRoleProfile` 新增层级感知模板：L2/L4 在职责、常见技能、摘要中明确区分；任务 #17 实测 `Java` 的 L2/L4 画像内容已分化，不再仅层级与样本数不同）                                                                                                                                     | P0     | 待分配 | 2026-04-04 | 同标题不同层级画像具备可解释差异，不再出现“内容几乎一致”问题                 |
| T47  | 前后端 | 人工岗位画像入库与前台直出              | 已完成首版（新增 `manual-job-portraits-seed` 脚本将 10 条岗位画像写入 PostgreSQL；后端新增 `/api/v2/job-portraits/manual`；前端岗位画像页改为读取该接口并展示 7 维指标明细）                                                                                                                                 | P0     | 待分配 | 2026-04-04 | 前端岗位画像中心稳定展示数据库中的 10 条人工岗位画像                         |
| T48  | 前端   | 前台 dark 主题移除并统一 light 模式     | 已完成（移除 `AppLayout` 暗色切换按钮/状态与 `.dark` 覆盖样式；移除 `DashboardPage` 的 `html.dark` 样式分支；`index.html` 不再配置 `tailwind.config.darkMode`）                                                                                                                                              | P1     | 待分配 | 2026-04-04 | 前台页面不再存在 dark 模式入口，界面仅保留 light 主题                        |
| T49  | 前端   | 学生画像中心二选一重构（纯前端）        | 已完成并增强输入（`/profile` 页面改为“表单输入学生画像 / 简历上传”二选一交互；移除学生画像接口依赖，改为本地会话生成预览与记录列表；能力维度已改为用户输入并强校验，不再仅做静态展示）                                                                                                                       | P1     | 待分配 | 2026-04-04 | 不接后端时可独立演示输入、提交、预览与历史记录                               |
| T50  | 前端   | 岗位画像页新增履历信息便捷录入          | 已完成（在 `/job-profiles` 页面新增“个人履历信息录入”区块，覆盖基本信息/个人总结/教育背景/工作或项目经历/专业技能；支持动态增删条目、必填校验、本地草稿保存与一键复制结构化文本）                                                                                                                            | P1     | 待分配 | 2026-04-05 | 用户可在前端一次性整理完整履历并复制到简历制作流程                           |
| T51  | 前后端 | Agent 简历生成链路与新标签页打印        | 已完成（新增 `/api/v2/ai/resume-html`：后端使用独立简历生成提示词调用 Agent，返回可打印 HTML；前端岗位画像页新增“交给 Agent 生成简历”按钮，提交履历信息后在新标签页打开结果并支持打印）                                                                                                                      | P1     | 待分配 | 2026-04-05 | 用户提交履历后可直接获得可打印 HTML 简历并在浏览器新标签页查看               |
| T52  | 后端   | 简历生成接口超时保护与快速失败          | 已完成（`ai-resume.runtime` 新增超时保护与 turn_end 错误捕获，`session.prompt` 超时统一抛出 `504/AI_RESUME_HTML_TIMEOUT`；新增 `[ai-resume]` 与 `[http-error]` 日志，且支持 `AGENT_RESUME_TIMEOUT_MS` 配置化调参）                                                                                           | P1     | 待分配 | 2026-04-05 | 简历生成超时时接口可在预期时间内返回明确错误，且后端日志可直接定位失败点     |
| T53  | 前后端 | 简历生成结果落库与历史查看              | 已完成（新增 `ai_resume_html_records` 表与 `/api/v2/ai/resume-html` 列表/详情接口；简历生成成功后自动落库并返回 `resume_id`；前端岗位画像页新增“已生成简历”列表，支持点击记录后在新标签页打开）                                                                                                              | P1     | 待分配 | 2026-04-05 | 用户可在前端查看历史简历记录并按条目回放到新标签页                           |
| T54  | 前端   | 简历生成可见性兜底（弹窗拦截场景）      | 已完成（`JobProfilesPage` 在点击时预先打开窗口并在异步完成后写入 HTML，显著降低“生成成功但看不到简历”问题；同时新增当前页内嵌 `iframe` 预览兜底，弹窗被拦截时自动展示生成结果）                                                                                                                              | P1     | 待分配 | 2026-04-05 | 即使浏览器拦截新窗口，用户仍可在当前页面直接看到已生成简历                   |
| T55  | 前端   | 学生画像中心接入简历生成与历史画像      | 已完成（`ProfilePage` 新增 Agent 简历生成区、历史简历读取与弹窗拦截预览兜底；画像提交从本地演示切换为后端接口落库，并在同页展示数据库历史画像）                                                                                                                                                              | P0     | 待分配 | 2026-04-05 | 用户可在学生画像中心完成“生成简历 -> 生成画像 -> 查看历史画像/历史简历”闭环  |
| T56  | 前端   | 取消简历新标签打开并统一页内预览        | 已完成（`ProfilePage` 与 `JobProfilesPage` 已移除 `window.open` 打开新标签逻辑；简历生成与历史查看统一改为当前页 iframe 预览）                                                                                                                                                                               | P1     | 待分配 | 2026-04-05 | 用户查看简历不再跳出新标签，统一在当前页面完成预览                           |
| T57  | 后端   | 简历上传文本编码清洗与 UTF8 兜底        | 已完成（`profile.route` 对上传简历文本增加控制字符清洗与空文本拦截，`knowledge.service` 入库前再次清洗；修复 `invalid byte sequence for encoding "UTF8": 0x00`）                                                                                                                                             | P0     | 待分配 | 2026-04-05 | 简历上传生成画像链路不再因 `0x00` 控制字符触发 PostgreSQL UTF8 写入错误      |
| T58  | 前端   | 学生画像双路径互斥与能力等级下拉化      | 已完成（手动模式保留表单输入且不再要求上传简历；简历模式仅保留上传入口并移除额外手填项；创新/学习/抗压/沟通/实习能力改为等级下拉选择并映射到画像评分字段）                                                                                                                                                   | P0     | 待分配 | 2026-04-05 | 用户可清晰选择“手动填写”或“简历上传”单一路径，能力项以等级下拉便捷录入       |
| T59  | 前端   | 岗位画像中心移除个人履历录入模块        | 已完成（`JobProfilesPage` 已删除个人履历录入、草稿保存与简历生成相关状态/交互，仅保留岗位画像分类筛选、列表浏览与详情展示）                                                                                                                                                                                  | P1     | 待分配 | 2026-04-05 | 岗位画像中心聚焦岗位画像展示，不再包含学生履历录入入口                       |
| T60  | 后端   | 1W 流水线重构为清洗+Agent十画像入库     | 已完成首版（`jobs-intelligence` 流水线已重构为“全量岗位清洗入 `v2_pipeline_cleaned_jobs` -> 调用 Pi Agent 生成 10 条岗位画像 -> 写入 `v2_agent_job_portraits` 并同步 `v2_manual_job_portraits` 展示表”；任务成功消息含模型与 trace_id）                                                                      | P0     | 待分配 | 2026-04-05 | 运行 `jobs:pipeline:run` 后可在数据库看到清洗快照与 10 条 Agent 岗位画像结果 |
| T61  | 前端   | 数据处理中心阶段反馈与进度可视化        | 已完成（`DataPipelinePage` 新增清洗阶段/Agent 生成阶段识别、自动轮询提示、运行态按钮文案与进度条；任务执行中明确显示“清洗中/Agent生成中”，避免用户误判无响应）                                                                                                                                               | P0     | 待分配 | 2026-04-05 | 点击“启动流水线”后页面可持续反馈当前阶段与进度，执行期间按钮状态可见且可感知 |
| T62  | 前端   | 数据处理中心动态状态动效优化            | 已完成（新增阶段实时跳动文案、时间线节点高亮与流动进度条 shimmer 动效；运行中“排队/清洗/Agent生成/完成”阶段可动态感知，强化长任务反馈体验）                                                                                                                                                                  | P1     | 待分配 | 2026-04-05 | 用户在执行长任务时可通过动态动效持续感知系统仍在工作，减少“卡住”误判         |
| T63  | 前端   | 数据处理中心命名语义重设计              | 已完成（页面标题、区块标题、模式文案与任务字段标签统一改为“清洗入库 + Agent 生成画像”语义；移除旧“事实抽取/标准岗位聚合”表述，保留动态状态展示能力）                                                                                                                                                         | P1     | 待分配 | 2026-04-05 | 用户可从页面命名直接理解当前流程，不再出现功能语义与展示文案不一致           |
| T64  | 全链路 | 流水线 mode 枚举语义迁移                | 已完成（`JobPipelineMode` 从 `facts_canonical_full` 正式迁移为 `cleanse_agent_portraits`；前端默认值、OpenAPI、后端 schema 与脚本参数同步更新；后端入参保留旧值自动映射兼容）                                                                                                                                | P0     | 待分配 | 2026-04-05 | 页面与接口不再暴露旧模式术语，历史调用传旧值仍可运行且自动转新值             |
| T65  | 前后端 | 手工画像一键生成职业图谱                | 已完成首版（后端新增 `POST /api/v2/career-paths/generate`，强制读取 `v2_manual_job_portraits` 作为节点来源；按岗位族/级别/技能重合度召回候选并经内置 Agent 判定后写入 Neo4j；前端路径图谱中心新增“生成图谱”按钮并支持成功提示与自动刷新）                                                                    | P0     | 待分配 | 2026-04-05 | 用户在前端可一键生成图谱并立即查询展示，图谱包含晋升/换岗/技能迁移关系       |
| T66  | 前端   | 路径图谱目标岗位查询源切换到手工画像表  | 已完成（`/career-paths` 目标岗位下拉改为读取 `/api/v2/job-portraits/manual`，即 `v2_manual_job_portraits`；页面按岗位名称映射 `job_id` 后发起图谱查询，不再以 jobs 列表作为下拉数据源）                                                                                                                      | P0     | 待分配 | 2026-04-05 | 目标岗位查询条件的数据来源明确来自 `v2_manual_job_portraits`                 |
| T67  | 前后端 | 路径图谱查询参数报错修复                | 已完成（修复前端误用 `fetchJobs(10000)` 触发“岗位查询参数不合法”；改为后端手工画像接口直接返回 `job_id` 并由前端直接使用，彻底移除超限 jobs 查询依赖）                                                                                                                                                       | P0     | 待分配 | 2026-04-05 | 打开路径图谱页面不再出现参数校验报错，目标岗位可正常选择并查询               |
| T68  | 前后端 | Agent 推理生成职业路径图谱              | 已完成首版（新增 `jobs-intelligence.graph.agent.ts` Agent 图谱生成器；contracts 和 schema 新增 `use_agent` 参数与 `generation_mode` 返回字段；service 层支持 Agent/规则双模式切换，Agent 失败自动降级规则版；前端路径图谱中心新增 Agent 推理复选框开关与按钮文案动态区分；全链路 `npm run type-check` 通过） | P0     | 待分配 | 2026-04-05 | 用户可在前端切换 Agent 推理模式生成图谱，Agent 生成的 reason 含具体技能依据  |
| T69  | 前后端 | 路径图谱目标岗位与岗位画像中心口径对齐  | 已完成（修复 `generateCareerPathGraph` 规则模式的岗位 ID 映射来源：从 `pipelineJobs` 切换为 `v2_manual_job_portraits` 记录中的 `job_id`；新增冲突映射回归测试，确保同名岗位优先使用画像表映射，避免图谱中心岗位与岗位画像中心不一致）                                                                        | P0     | 待分配 | 2026-04-05 | 路径图谱中心目标岗位与岗位画像中心展示同一岗位口径，映射冲突时不再错配       |
| T70  | 前后端 | 用户数据直出图谱与双图展示              | 已完成首版（后端新增 `POST /api/v2/job-portraits/manual/seed` 直接写入用户给定画像；前端职业路径页改为 ECharts 匹配度雷达 + G6 岗位关联/晋升图谱双视图，并保留图谱查询与详情联动）                                                                                                                           | P0     | 待分配 | 2026-04-05 | 用户可一键入库并在同一页面看到雷达匹配与关系图谱                             |
| T71  | 前端   | 学生画像页简历生成按需展开              | 已完成（学生画像中心“简历生成”区块改为默认收起，新增“展开简历生成/收起简历生成”开关；仅在展开状态下展示简历生成表单与预览，历史简历列表保持常驻可查看）                                                                                                                                                      | P1     | 待分配 | 2026-04-07 | 默认进入页面不展示简历生成表单，用户点击展开后可正常填写并生成简历           |
| T72  | 前后端 | 职业报告中心新增 Markdown 导出          | 已完成（扩展 contracts 与 OpenAPI 导出格式为 `pdf/markdown`；后端报告导出新增 Markdown 生成与下载 `Content-Type` 适配；前端报告页新增“导出 Markdown”按钮并在归档列表按格式展示标签）                                                                                                                         | P1     | 待分配 | 2026-04-07 | 用户可在职业报告中心导出 PDF 或 Markdown，归档列表可正确识别导出格式         |
| T73  | 工程   | 跨电脑迁移导出/导入脚本                 | 已完成（新增 `scripts/migration-export.sh`、`scripts/migration-import.sh` 与 Windows 专用 `scripts/migration-import.ps1`；支持迁移仓库工作副本、PostgreSQL 备份、前后端 `.env`、`~/.career-agent/pi-agent` 与报告导出目录，并在新电脑自动安装依赖与 Playwright Chromium）                                    | P1     | 待分配 | 2026-04-12 | 更换电脑时可按统一脚本完成主链路迁移，Windows 原生环境也可直接恢复           |
| T74  | 后端   | AI 润色无关内容清洗                     | 已完成（`ai-polish.runtime` 强化润色系统提示词，要求只输出润色后的正文；允许正文使用 Markdown 层级、列表、加粗等格式，但禁止“无需调用任何工具”“润色后文本：”“我来润色……”等说明性前缀和“如需调整风格…”尾部追问；新增回归测试覆盖 Markdown 保留与无关外壳清理）                                                | P0     | 待分配 | 2026-04-17 | 报告章节经 AI 润色后可保留正文 Markdown，但不再写入模型说明或尾部追问        |
| T75  | 前后端 | 岗位画像四格漫画 MVP                    | 已完成首版（contracts 增加漫画图片字段；后端通过 `baoyu-imagine` 生成并保存岗位画像四格漫画，图片经 `/assets/job-comics` 静态展示；前端岗位画像详情支持生成、展示与重新生成）                                                                                                                                | P1     | 待分配 | 2026-04-17 | 用户可在岗位画像中心为单个岗位生成漫画，刷新后仍能看到已保存图片             |
| T76  | 后端   | 岗位漫画 prompt 岗位差异化              | 已完成（`jobs-intelligence.comic.ts` 新增岗位类型场景规则，按前端传入岗位名称/分类/简介/技术栈/职责匹配前端、后端、测试、运维、数据、算法、设计、技术支持、实施、硬件测试、产品、网络销售等不同漫画脚本；新增纯函数回归测试防止所有岗位退化为写代码场景）                                                    | P1     | 待分配 | 2026-04-17 | 不同岗位生成漫画时会展示对应真实任务、核心动作、协作对象和交付结果           |

## 5. 风险与应对

1. 风险：岗位原始数据字段不统一。  
   应对：增加字段别名映射与清洗规则，保留原始 payload。
2. 风险：Agent 输出受外部模型供应商状态影响。  
   应对：保留最小 `agent:smoke` 自检脚本，并通过模板化报告与步骤轨迹降低单次调用波动的影响。
3. 风险：准确率达标压力。  
   应对：先建立评测集与误差分析闭环，持续迭代权重和提示词。

## 6. 更新规则

1. 每完成一个任务，更新“状态/计划完成/验收标准/备注”。
2. 每次联调后，补充问题清单与修复记录。
3. 每周输出一次里程碑进度总结。
4. 每次问题解决后，必须追加结构化记录到 `docs/问题记录库.jsonl`，格式遵循 `docs/问题记录规范.md`。
